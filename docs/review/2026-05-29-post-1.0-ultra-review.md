# Ultra Review — club-unison post-1.0 GA (2026-05-29)

> 3 系統並行レビュー（dimension-sliced workflow + Moody Blues holistic + Purple Haze deep-dive）を統合。
> lib 基準（panic / API leak / 無制限リソース / 入力堅牢性は重大度+1）+ IPv6-first 前提で評価。

## サマリ

| 系統 | 方式 | 結果 |
|---|---|---|
| review workflow | 10 観点 sweep × 独立 skeptic 反証 | 53 raw → **44 confirmed** (Major 8 / Minor 31 / Idea 5) / 9 refuted |
| Moody Blues | holistic + CI gate | CI **完全グリーン** (221 pass / 19 ignored)、issue 5（全て workflow と重複） |
| Purple Haze | network 並行性 / Hailing α / security 深掘り | confirmed 3（SNI Major は**固有**）+ 無害判定多数 |

CI: `cargo clippy --lib --workspace -D warnings` PASS / `cargo test --workspace` 221 pass。
（`--all-targets` で style warning 7 件 = tests/benches 6 + `unison-cli/schema_lint.rs:89` collapsible-if 1、いずれも Minor）

---

## ✅ 修正結果（2026-05-29 本セッションで全 Major 対応）

全 9 Major 修正済。`cargo clippy --lib --workspace -D warnings` クリーン、
`cargo test --workspace --include-ignored` 全 pass（lib 153 + 全 integration、0 failure）。

| # | 対応 |
|---|---|
| M1 | `Service`/`Method`/`MethodMessage`/`Stream` 型・`Protocol.services`・legacy channel `send/recv/error` を削除。test を新 `request`/`event`/`returns` 構文へ移行 |
| M2 | `src/context/`（CGP 全 876行）+ `pub mod context` + CGP 専用 test 2 ファイル削除。packet error-path test は温存 |
| M3 | `quic.rs` の `connect(addr,"localhost")` を `sni_server_name()` で実ホスト/IP 導出に変更。dev cert SAN に `127.0.0.1` 追加。unit 7 本追加 |
| M4 | MCP trust を endpoint-aware default（loopback→Skip / remote→System、secure-by-default）に。`#[default] Skip` 撤去 |
| M5 | 未使用 deps 12 個（cgp, cgp-component, proc-macro2, quote, syn, convert_case, crc32fast, indexmap, scc, miette 等）+ build-deps 整理 |
| M6 | channel demux loop の event/raw 配送を `try_send`（full 時 drop+warn）に。応答経路を slow consumer の HoL から隔離 |
| M7 | `connect()`/`disconnect()` で datagram dispatcher cache を invalidate。reconnect 後に新 connection で再 spawn |
| M8 | dispatch を `all_tools()` と同一 first-wins exact-match（`resolve_synth_tool`）に統一。fuzzy な `resolve_tool_name`/`parse_tool_name` を削除 |
| M9 | tool 名を `[A-Za-z0-9_]`+長さ上限で sanitize、description は制御文字 strip+上限、synthesized tool 数 256 上限。unit 5 本追加 |

追加テスト: `tests/test_medium_ip_dualstack.rs`（IPv4/IPv6/混在の sociable 疎通 5 本）。
フォローアップ: mesh Custom trust の実SAN E2E sociable test（M3 真の回帰テスト、別途）。

---

## 🔴 Major 修正キュー（cross-validation 済、上から着手）

| # | 問題 | location | 出典 | 信頼度 |
|---|---|---|---|---|
| **M1** | Legacy `service`/`method`/`stream`/`MethodMessage` KDL 型が frozen 1.0 public API に残存（dead、CLAUDE.md「Legacy残さない」違反） | `parser/schema.rs:49-50,242-295` | WF#1 + MB#1 | ×2 / 92 |
| **M2** | CGP `context` モジュールが public + 大（~876行）だが production consumer ゼロ | `lib.rs:55`, `context/{mod,adapter,handlers}.rs` | WF#2 + MB#2 | ×2 / 88 |
| **M3** | TLS server-name が `"localhost"` 固定 → mesh CA 実 SAN 接続が常に失敗 / `localhost` SAN なりすまし可（name-pinning 不全） | `quic.rs:330` | **PH固有** | 92 |
| **M4** | MCP bridge の `TrustMode::Skip` が `#[default]`（lib default-insecure） | `unison-mcp/config.rs:38` | WF#7 + MB#4 + PH | ×3 / 77 |
| **M5** | unison-protocol が dead dependency 10 個を宣言（cgp, cgp-component, proc-macro2, quote, syn, convert_case, crc32fast, indexmap, scc, miette） | `unison-protocol/Cargo.toml:25-86` | WF#3 | 88 |
| **M6** | channel recv loop が bounded `event_tx.send().await` でブロック → 全 in-flight request が HoL stall | `network/channel.rs:81` | WF#4 | 85 |
| **M7** | client datagram dispatcher が初回接続に bind され reconnect 後リセットされず silently dead | `network/client.rs:267` | WF#5 | 85 |
| **M8** | `all_tools()` の衝突 dedup と `handle_synthesized()` の解決が食い違い → silent wrong dispatch | `unison-mcp/tools.rs:86-116 vs 322-373` | WF#6 | 85 |
| **M9** | remote KDL の request 名/説明が無検証で synthesized MCP tool 名/説明へ流入（prompt injection） | `unison-mcp/mapping.rs:119-133` | WF#8 | 85 |

---

## 🟡 Minor（抜粋・cross-validated 優先）

- **dead public module `core`**（UnisonMessage 等、consumer ゼロ） `core/mod.rs` — WF#9 + MB#3 ×2
- **`DynamicProtocol::fetch` が `document.hash` を未検証**（integrity check 不在） `dynamic.rs:82` — PH#2
- **client unbounded mpsc が drain されず untrusted server からの bidi 連打で OOM** `quic.rs:136/349` — PH#3 + WF#17
- version/型 truncation: `header.rs:242-246` (u32→u8/u16 で compat gate すり抜け) WF#36 / `frame.rs:39-48` (≥4GiB 長さ prefix 黙々 truncate) WF#38 / `serialization.rs:202-240` (uncompressed payload が max で bound されない) WF#39
- stale version 文字列: TS `index.ts:112` "1.0.0-rc.1" WF#32 / Ruby `lib.rs:117` "1.0.0-rc.1" WF#27
- TS: `package.json` exports 順 (`import` が `types` の前) WF#33 / keywords に "rpc" 残存 WF#34 / `request()` が write reject 時 `#pending` leak WF#35
- Ruby: panic が Ruby プロセス全体を abort `ext/.../lib.rs:49-88` WF#28 / `recv()` が timeout なし uninterruptible WF#29
- MCP: synthesized tool 数に上限なし（DoS） `tools.rs:96-114` WF#31
- dead code: `datagram_dispatcher.rs:64`（v0.10.0 leftover 5項目）WF#19 / `parser/mod.rs:27` type_registry 未読 WF#20
- テスト欠落: network の conn/conn_quinn/frame/stream/cert/channel/dispatch に unit test なし WF#21

## 💡 Idea

- 過長モジュール: `quic.rs`(685) / `server.rs`(630) 分割 WF#41、`mapping.rs`(563)
- `FieldType::Object` が properties 無し `{"type":"object"}` で LLM に不透明 WF#42
- Cargo `exclude` の noop entries WF#40

## 反証で落ちた主な指摘（記録）

- discovery `format` 無視（実害なし）/ identity oneshot stale 消費（reconnect 経路で発生せず）/ Ruby insecure 固定（loopback gate で緩和）/ ServerIdentity 無制限フィールド（frame 8MB 上限内）/ trust.rs SkipVerification（loopback gate 済）

---

## 補足: テスト整備（本レビューと並行で実施）

- `tests/test_medium_ip_dualstack.rs` 新規: IPv4/IPv6/混在の sociable 疎通テスト 5 本（IPv6-first）。全 pass。
